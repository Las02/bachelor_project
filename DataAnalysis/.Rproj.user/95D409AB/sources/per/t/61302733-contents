---
title: "Visualization of ribdif/bacdive/growthrate data"
output: html_document
date: "2023-03-27"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(naniar)
library(tidyverse)
```

## Purpose

The goal of this section is the prepare visualizations of the data,
and it's distributions 

## Reading in the data
The data is read from the R data file create from 01_ReadInData.Rmd

```{r}
D <- readRDS("./data/bacdive_growth_ribdif.rds")
D
```


## The Nans in the data

Visualize the missing values in the data

```{r}


D %>% 
  select(antibiotics, motility, sporeforming, max_growth_min, oxygen.tolerance) %>% 
  gg_miss_upset(nsets=5)

D %>% 
  select(antibiotics, motility, sporeforming, max_growth_min, oxygen.tolerance,pathogen_animal,pathogen_human) %>% 
  vis_miss() +
  ggtitle("Missing data visualized")
```

